# Environment variables example for TableTop Studio Backend

# --- Server Configuration ---
# Port the backend server will run on
PORT=5000

# --- Database ---
# Your MongoDB connection string (replace with your actual URI in .env)
# Example format for local MongoDB: MONGODB_URI=mongodb://localhost:27017/yourDatabaseName
# Example format for MongoDB Atlas: MONGODB_URI=mongodb+srv://<username>:<password>@<cluster-url>/<dbName>?retryWrites=true&w=majority
MONGODB_URI=

# --- Security ---
# A strong, random secret key for signing JSON Web Tokens (JWT)
# Generate a secure secret for your actual .env file (e.g., using a password generator)
JWT_SECRET=

# --- Node Environment ---
# Set to 'development', 'production', or 'test'
# Affects things like error detail levels, logging, etc.
NODE_ENV=development